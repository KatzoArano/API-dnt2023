import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hemengo Distrib',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
