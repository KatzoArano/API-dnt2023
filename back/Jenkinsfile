pipeline {
    agent any

    stages {
        stage('Chekout') {
            steps {
                git url: 'https://github.com/KatzoArano/API-dnt2023', branch: 'main'
                echo 'Checkout Completed'
            }
        }
           stage('Build') {
            steps {
                sh './ci-cd/build.sh'
                echo 'Building..'

            }
        }
        stage('Test') {
            steps {
                sh './ci-cd/test.sh'
                echo 'Testing..'

            }
        }
        stage('Deploy') {
            steps {
                sh './ci-cd/deploy.sh'
                echo 'Deploying....'
            }
        }
    }
}